export default function TodolistBody() {
  return (
    <p className="font-body">
      An interactive web app that allows the user to create tasks with an
      expandable details section and a target completion date. Tasks are stored
      as state using Context and can be moved between three lists.
    </p>
  );
}
