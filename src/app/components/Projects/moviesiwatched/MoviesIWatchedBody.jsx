export default function MoviesIWatchedBody() {
  return (
    <p className="font-body">
      A full stack web app built with Node.js and React.js. Users must register
      and login before adding to their list of rated movies that are stored in a
      database. CRUD functionality to store a list of movies the user has
      watched.
    </p>
  );
}
