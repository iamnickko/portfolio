export default function ExpenseTrackerBody() {
  return (
    <p className="font-body">
      A web app that uses React Router (loaders & actions) to dynamically
      display budgets and associated expenses that are stored in the browser.
    </p>
  );
}
